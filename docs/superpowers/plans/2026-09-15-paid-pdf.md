# Paid PDF previews and downloads

User requirements: preview the exact admin-uploaded PDF, show the first two pages, blur/lock the remainder, and require Razorpay payment for the full original download.

- Generate at most two raster preview images on the server. Public routes never return the full PDF or later pages. Validate new uploads by parsing them; existing uploads are previewed on demand.
- Create Pending local orders at server catalogue prices. Create Razorpay orders on the server, verify checkout HMAC and fetch captured payment details before granting Paid access. Verify webhook signatures and reconcile captured events. Legacy demo orders cannot download.
- Replace the public sample modal with actual page images and blurred placeholders. Add Razorpay checkout, paid downloads, pending-order retry, and accurate unavailable/test-mode states.
- Keep uploaded originals unchanged; persist student browser sessions for 30 days through restart. No email-only recovery granting another person's orders.
- Verify preview page limits, invalid PDFs, payment signature/status/amount/ownership, webhook handling, unchanged paid-download bytes, storefront/admin browser flows and production build. Document server-only credentials and actual live-payment setup limitation.
